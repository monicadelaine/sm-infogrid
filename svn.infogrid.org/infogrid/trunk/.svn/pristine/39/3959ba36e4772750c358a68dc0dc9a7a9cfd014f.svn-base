<html>
 <head>
  <title>package org.infogrid.jee.taglib.mesh.set</title>
 </head>
 <body>
  <p>Tags to process <code>MeshObjectSets</code>.</p>
  
  <h1>Tags that test the content of a <code>MeshObjectSet</code></h1>
  <p>{@link org.infogrid.jee.taglib.mesh.set.IfSetEmptyTag IfSetEmptyTag} processes its content
     if a <code>MeshObjectSet</code> is empty.
     {@link org.infogrid.jee.taglib.mesh.set.IfSetNotEmptyTag IfSetNotEmptyTag} processes its content
     if a <code>MeshObjectSet</code> is not empty.</p>
  <p>The following tag attributes are provided:</p>
  <table class="infogrid-border">
   <thead>
    <tr>
     <th width="15%">Attribute</th>
     <th width="60%">Meaning</th>
     <th>Required?</th>
    </tr>
   </thead>
   <tbody>
    <tr>
     <td><code>meshObjectSetName</code></td>
     <td>Name of the bean in the scope that is the <code>MeshObjectSet</code> to consider.</td>
     <td>required</td>
    </tr>
    <tr>
     <td><code>filter</code></td>
     <td>If true, filter output for characters that are sensitive in HTML.</td>
     <td>optional</td>
    </tr>
    <tr>
     <td><code>ignore</code></td>
     <td>If true, ignore missing beans and simply output nothing. Otherwise, when beans are missing,
         throw a <code>JspException</code></td>
     <td>optional</td>
    </tr>
    <tr>
     <td><code>scope</code></td>
     <td>The scope (page, request, session, application) in which beans are being looked up</td>
     <td>optional</td>
    </tr>
   </tbody>
  </table>

  <h1>Tags that iterate over the content of a <code>MeshObjectSet</code></h1>
  <p>{@link org.infogrid.jee.taglib.mesh.set.MeshObjectSetIterateTag MeshObjectSetIterateTag} iterates
     over the content of a <code>MeshObjectSet</code> that is given as a bean.
     {@link org.infogrid.jee.taglib.mesh.set.MeshObjectSetTraverseIterateTag MeshObjectSetTraverseIterateTag} iterates
     over the content of a <code>MeshObjectSet</code> that is given as a traversal from a start <code>MeshObject</code>
     via a given <code>TraversalSpecification</code>.</p>
  <p>These main tags are being complemented by tags that categorize the content contained in either main tag:
     {@link org.infogrid.jee.taglib.mesh.set.MeshObjectSetIterateContentRowTag MeshObjectSetIterateContentRowTag}
     identifies that contained content that is to be rendered once per <code>MeshObject</code> contained
     in the <code>MeshObjectSet</code>.
     {@link org.infogrid.jee.taglib.mesh.set.MeshObjectSetIterateNoContentTag MeshObjectSetIterateNoContentTag}
     identifies the contained content that is to be rendered if the <code>MeshObjectSet</code> is empty.
     {@link org.infogrid.jee.taglib.mesh.set.MeshObjectSetIterateHeaderTag MeshObjectSetIterateHeaderTag}
     identifies the contained content that is to be rendered as the header row.</p>
  <p>{@link org.infogrid.jee.taglib.mesh.set.MeshObjectSetIterateTag MeshObjectSetIterateTag}
     provides the following tag attributes:</p>
  <table class="infogrid-border">
   <thead>
    <tr>
     <th width="15%">Attribute</th>
     <th width="60%">Meaning</th>
     <th>Required?</th>
    </tr>
   </thead>
   <tbody>
    <tr>
     <td><code>meshObjectSetName</code></td>
     <td>Name of the bean in the scope that is the <code>MeshObjectSet</code> to consider.</td>
     <td>required</td>
    </tr>
    <tr>
     <td><code>loopVar</code></td>
     <td>Name of the bean that contains the current <code>MeshObject</code> in the iteration.</td>
     <td>required</td>
    </tr>
    <tr>
     <td><code>filter</code></td>
     <td>If true, filter output for characters that are sensitive in HTML.</td>
     <td>optional</td>
    </tr>
    <tr>
     <td><code>ignore</code></td>
     <td>If true, ignore missing beans and simply output nothing. Otherwise, when beans are missing,
         throw a <code>JspException</code></td>
     <td>optional</td>
    </tr>
    <tr>
     <td><code>scope</code></td>
     <td>The scope (page, request, session, application) in which beans are being looked up</td>
     <td>optional</td>
    </tr>
   </tbody>
  </table>
  <p>{@link org.infogrid.jee.taglib.mesh.set.MeshObjectSetTraverseIterateTag MeshObjectSetTraverseIterateTag}
     provides the following tag attributes:</p>
  <table class="infogrid-border">
   <thead>
    <tr>
     <th width="15%">Attribute</th>
     <th width="60%">Meaning</th>
     <th>Required?</th>
    </tr>
   </thead>
   <tbody>
    <tr>
     <td><code>startObjectName</code></td>
     <td>Name of the bean in the scope from which the traversal starts. Must be a <code>MeshObject</code>.</td>
     <td>required</td>
    </tr>
    <tr>
     <td><code>traversalSpecification</code></td>
     <td>External form of the <code>TraversalSpecification</code> to use from the <code>MeshObject</code> identified
         by <code>startObjectName</code>.</td>
     <td>required</td>
    </tr>
    <tr>
     <td><code>loopVar</code></td>
     <td>Name of the bean that contains the current <code>MeshObject</code> in the iteration.</td>
     <td>required</td>
    </tr>
    <tr>
     <td><code>filter</code></td>
     <td>If true, filter output for characters that are sensitive in HTML.</td>
     <td>optional</td>
    </tr>
    <tr>
     <td><code>ignore</code></td>
     <td>If true, ignore missing beans and simply output nothing. Otherwise, when beans are missing,
         throw a <code>JspException</code></td>
     <td>optional</td>
    </tr>
    <tr>
     <td><code>scope</code></td>
     <td>The scope (page, request, session, application) in which beans are being looked up</td>
     <td>optional</td>
    </tr>
   </tbody>
  </table>
  <p>{@link org.infogrid.jee.taglib.mesh.set.MeshObjectSetIterateHeaderTag MeshObjectSetIterateHeaderTag}
     provides no attributes.</p>
  <p>{@link org.infogrid.jee.taglib.mesh.set.MeshObjectSetIterateNoContentTag MeshObjectSetIterateNoContentTag}
     provides no attributes.</p>
  <p>{@link org.infogrid.jee.taglib.mesh.set.MeshObjectSetIterateContentRowTag MeshObjectSetIterateContentRowTag}
     provides no attributes.</p>
 </body>
</html>
